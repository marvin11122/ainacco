{
  "name": "ai-tui-shell",
  "version": "1.0.0",
  "description": "Minimal Electron-free Monaco + xterm.js + node-pty shell (Express + WebSocket).",
  "license": "MIT",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "copy:assets": "mkdir -p public/monaco public/xterm && cp -r node_modules/monaco-editor/min public/monaco && cp -r node_modules/xterm/lib public/xterm && cp node_modules/xterm/css/xterm.css public/xterm/",
    "postinstall": "npm run copy:assets"
  },
  "dependencies": {
    "express": "^4.19.2",
    "monaco-editor": "^0.52.2",
    "node-pty": "^1.0.0",
    "ws": "^8.18.0",
    "xterm": "^5.3.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
